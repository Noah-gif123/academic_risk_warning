# v2 重构 · 改造步骤与阶段

> 配套文档：`下一阶段改造清单.md`（改什么）｜本文（按什么顺序改、分成几个阶段）
> 本文不排工期，只用体量标记：**S**（小，半天级）｜**M**（中，数天级）｜**L**（大，需连续投入）

---

## 0. 先回答你的想法：AI 暂时停掉 —— 赞成，但要"分层停"，不要"注释掉"

### 0.1 为什么赞成

1. **你的新算法层与 LLM 完全正交**：预警生成（`WarningGenerationService`）是纯规则计算——我实测过，它只读 `class_performance / homework_info / score_info / knowledge_mastery`，**不碰任何大模型**；`task` 包（每日快照、超时升级、催办）也**零 agent 依赖**。所以停 AI 不会让"预警"这个核心功能少一根毛。
2. **LLM 会掩盖数据库问题**：现在很多页面"看起来有内容"，其实是 LLM 现编的解读。停掉之后，哪里缺数据、哪里口径不对，会立刻暴露——这正是重构期需要的反馈。
3. **成本和心智负担**：每次调试都要等 30–50 秒的模型调用，且输出不稳定；停掉后迭代速度提升明显。

### 0.2 停掉 AI 之后，系统还剩什么（先看清代价）

| 功能 | 依赖 | 停 AI 后状态 |
|---|---|---|
| 预警生成（每日快照、红橙黄绿） | 纯规则 | ✅ 正常 |
| 风险雷达图 / 趋势图 / 班级均分 | 快照 + 预警表 | ✅ 正常 |
| 定时任务（快照、超时升级、催办、归档提醒） | 无 agent 依赖 | ✅ 正常 |
| 预警处理、干预记录（人工录入）、通知 | 规则 + 表 | ✅ 正常 |
| 知识点薄弱项、推荐问题、预警规则说明 | 纯数据/配置 | ✅ 正常 |
| 三维学情画像**查看** | 读 `student_profile` | ✅ 可看历史生成结果 |
| 三维画像**生成** | ProfileAgent（LLM） | ❌ 按钮需隐藏 |
| 分析报告 / 归因 | AnalysisAgent（LLM） | ❌ |
| 学习计划 / 推荐、策略调整 | Recommend/StrategyAgent | ❌ 历史数据仍可查 |
| 学生 AI 助手、教师 AI 问答、工具问答 | RAG / 工具调用 | ❌ 入口需隐藏 |
| 干预方案生成、教学建议生成 | LLM | ❌ 按钮需隐藏 |
| 智能体运行历史 / golden set 评测 | `agent_run` 表 | ✅ 可看历史；评测按钮隐藏 |
| 教师「课程风险概览」 | 数据（规则）+ AI 解读 | ⚠️ **需改造**：见 0.4 |

### 0.3 停机方式（不要注释代码）

注释代码会造成三个后果：合并冲突、忘记恢复、无法一键回滚。正确做法是**三层开关 + 分支冻结**：

```
① 代码冻结：给当前 AI 成果打 tag（v1.3.0-ai-complete）+ 保留 master 分支不动
            重构在新分支（v2-rebuild）上进行，AI 代码原样留在库里，只是不调用
② 后端开关：application.properties → agent.enabled=false（已存在，无需新写）
③ 前端开关：新增一个 feature flag，统一隐藏所有 AI 入口（见 0.4-②）
```

回滚成本 = 改一行配置 + 打开一个 flag。

### 0.4 具体改动清单（停 AI 的真实工作量：约 S 体量）

**① 后端 —— 让"数据端点"在 AI 关闭时仍能用（必须改，否则页面直接报错）**

| 文件 | 现状 | 改法 |
|---|---|---|
| `TeacherAgentController.getRiskOverview`（第 101 行） | LLM 解读与数据在同一个 `try` 里，AI 一关整个接口报错、教师看不到风险分布 | 把 `feedbackAgent.answerQuestion(...)` 单独包一层 `try/catch`；失败时 `aiAnalysis=null` + `aiEnabled=false`，**数据照常返回** |
| `TeacherAgentController.interventionPlan` / `teachingAdvice` | 纯 LLM | 关闭时返回 `{success:false, aiDisabled:true, message:"AI 功能已暂停"}` |
| `TeacherAgentController.qa` / `StudentAgentController.chat` / `AgentController.ask` | RAG / 工具调用 | 同上；`AgentOrchestrator` 已有 `disabledResult()` 可直接复用 |
| 建议新增 `GET /api/system/features` | — | 返回 `{aiEnabled:false}`，前端据此隐藏入口（比前端硬编码可靠） |

**② 前端 —— 隐藏 5 个 AI 入口（不删组件，只控制渲染）**

| 组件/位置 | 处理 |
|---|---|
| `ChatAssistant.vue`（教师 AI 助手整个面板） | `v-if="features.aiEnabled"` |
| `StudentChatAssistant.vue`（学生 AI 助手） | 同上 |
| `teacher/AgentToolAsk.vue`（工具问答） | 同上 |
| `teacher/AgentRunHistory.vue`（运行历史 + 评测） | 面板保留（可看历史）但隐藏"golden set 评测"按钮；或整体隐藏，二选一 |
| `TeacherStudentList.vue` 的「生成画像」按钮 | 隐藏按钮，画像卡片保留（显示 DB 里已有画像） |

**③ 配置**

```properties
agent.enabled=false          # 已存在，置 false 即可
# 可选：把 LLM 超时调小，避免误调用时长时间挂起
agent.llm-timeout=30
```

**④ 留着不动的（等 v2 收尾再回收）**

`BailianChatClient`、`BailianRAGClient`、六个 Agent、`ToolCallingLoop`、`DataQueryAgent`、`AgentRunService`、`AgentEvalService`、`ReflectionLoop`、`AgentOutputValidator`、`student_memory` —— 一行都不删。它们是 v1.3.0 的既有成果，也是答辩的"智能体亮点"，最后阶段只需**把工具与 prompt 指向新表**即可复活。

### 0.5 停 AI 期间对外的说法（避免被问住）

> "这一阶段我们在做数据库与规则引擎的重构，把判定逻辑从模型手里收回到确定性引擎；AI 层（解释、建议、问答）先冻结，等新数据模型稳定后再接回来——因为我们发现真正的问题是数据口径，不是模型。"

这个说法同时解释了"为什么现在页面 AI 少了"和"为什么这是个专业决策"，比"AI 先关掉了"体面得多。

---

## 1. 阶段总览（依赖顺序）

```
P0 冻结与降级 ──► P1 口径表 ──► P2 数据库地基 ──┬─► P3 考勤双轨 ──► P4 通道A ──► P6 导员端 ──► P7 通道B/C ──► P8 教务端/问卷 ──► P10 AI 回收
                                              │
                                              └─► P5 四端鉴权（可与 P3 并行）
                     P9 合成数据 + 回测（与 P2 之后任意阶段并行，越早越好）
```

| 阶段 | 目标 | 前置 | 体量 | 能否产出可演示内容 |
|---|---|---|---|---|
| **P0** | 停 AI、冻结 v1.3.0、拉新分支 | 无 | S | 现有演示仍可跑（功能变少） |
| **P1** | 口径表定稿（本文档配套 §1） | P0 | S | — |
| **P2** | 数据库 v2 地基（学期/组织/开课/选课/课次） | P1 | L | — |
| **P3** | 考勤双轨 + 覆盖率体检 | P2 | L | ★ 教师端 30 秒点名单 |
| **P4** | 通道 A（确定性风险）+ 扣分 + 硬门槛 | P3 | M | ★★ 学生端"剩余可缺次数"、红色确定性预警 |
| **P5** | 四端鉴权与账号统一 | P2 | M | 导员能登录 |
| **P6** | 导员端（聚合 + 升级链 + 干预留痕） | P4+P5 | L | ★★ 导员看板与班会提醒 |
| **P7** | 通道 B → 通道 C（Δ/c/W、影子分数） | P4 | L | ★ 班内排序、跨班对比 |
| **P8** | 教务端工作台 + 三级报表 + 问卷链路 | P3+P5 | L | ★ 归档工作台、覆盖率督办 |
| **P9** | 合成数据 + 参数回测（并行） | P2 | M | 论文实验表 |
| **P10** | AI 功能回收（接新表与新输出） | P4 起任意 | M | 智能体重新上线 |

---

## 2. 各阶段详细步骤

### P0 冻结与降级（S）

1. 打 tag / 备份：`v1.3.0-ai-complete`（现库 38 张表 + 133 个单测全绿的状态）。
2. 改 `agent.enabled=false`；按 §0.4 做后端 4 处降级改造 + 前端 flag（1 处接口 + 5 处渲染控制）。
3. 验证：教师端首页、学生列表、学生详情、预警面板、雷达/趋势图**全部可用**；AI 入口消失；`/api/agent/teacher/risk-overview` 仍返回分布数据。
4. 建新分支 `v2-rebuild`；把两份 SQL 收敛成一份 DDL（见 P2-0）。

**验收**：一次 `gradlew test` 全绿；页面上不再出现"AI 暂不可用"式报错。

### P1 口径表定稿（S）

把 `下一阶段改造清单.md` §1 的 12 条写成团队可引用的《口径表》，其中**必须包含**：1/3 线的精确表达（`N_max=floor(总课次/3)`，挂科 ⟺ `已缺 ≥ N_max+1`）、扣分无上限、覆盖率分母只数非停课课次、通道 A 三条判据、W 四档、导员阈值与多科目=3、engine_version 粒度。

**验收**：任何一条数字只有一个出处；后续所有代码注释都引用该表。

### P2 数据库 v2 地基（L）

0. **先收敛 SQL**：现在有两份（根目录 `study_warning_system.sql` 55KB 纯结构、`backend/sql/study_warning_system.sql` 935KB 含数据的 7 月版），先确定唯一真相，拆成 `sql/schema_v2.sql`（结构）+ `sql/seed_demo.sql`（种子数据）。
1. 建 `term`（学期）并在**所有**业务表带 `term_id`——这是当前最大缺口（38 张表里没有任何学期字段）。
2. 建组织结构：`administrative_class`、`counselor`、`counselor_class`、`teaching_class`、`teaching_class_admin_class`、`course_group`、`course_offering`。
3. 建 `enrollment`（学生×教学班×学期，状态 ACTIVE/WITHDRAWN/REPEAT/PENDING），替代 `student_course`；`student.class_name` → `admin_class_id`。
4. 建 `course_session`（一次课：周次/节次/日期/停课标记）——覆盖率分母的唯一真相。
5. 建 `score_item`（分项成绩）、`student_admission_score`（高考/入学分）。
6. 建 `risk_engine_config`、`teacher_style`、三张挂科率表。
7. 改造既有表：`score_info/homework_info/class_performance` 加 `enrollment_id/term_id`；`alert_*` 加 `engine_version/channel/confidence/...`；`teacher_class`、`attendance`、`student_course` 迁完即废弃。
8. 迁移脚本 + 对账（行数、分数合计、缺课合计三项）。
9. **顺手修的基础问题**（趁重构一次做掉）：密码统一 BCrypt 到 `sys_account`；`teacher_class` 的 ID 区间切人彻底废弃；`class_performance` 与 `attendance` 的重复语义合并为"明细为真相、汇总为缓存"。

**验收**：新库能表达"一门课 × 3 教学班 × 3 行政班 × 40 人 × 16 周 × 2 次课"的完整结构；旧数据 0 丢失。

### P3 考勤双轨 + 覆盖率（L）

1. 教师端：课表驱动的"一节课一张点名单"（默认全勤、只勾缺课/迟到/请假、约 30 秒）+ Excel 批量导入（多周补录）。
2. 学生端：平时考勤即时可见（明细列表）+ 扣分明细 + 申诉入口（只记录，不直达改数）。
3. 归档轨道：期中/期末窗口 → 教师端强制待办 → 教务收集统一录入 → 生成**归档考勤**（判定唯一依据）。
4. 覆盖率体检：`已录入课次 / 非停课应完成课次`；每两周轻量 + 期中正式；<1/2 → 提醒老师 + 教务督办列表。
5. 状态机（平时 → 期中归档 → 期末归档）+ 窗口配置（周次/时长/是否可延长）+ 退回重交。

**验收**：一节课 30 秒能录完；覆盖率能算对（含停课日）；归档后不可改、改动留痕。

### P4 通道 A + 扣分 + 硬门槛（M）

1. 规则引擎骨架 `RiskEngine` + engine_version 管理。
2. 通道 A 三条判据：① `已缺 ≥ N_max+1` ② `剩余可缺 ≤ 1` ③ `期末需多考 = (60−W+P)/r_f > 100`。
3. 扣分公式 `P = 2×缺课 + 1×迟到 + 1×max(0, 请假−3)`，`T = W − P` **不截断**。
4. 剩余可缺次数计算：`= 0` 显示"再缺一次即挂"，`≤ −1` 才"必然挂科"（**这是文档原表差的那一格**）。
5. 写入快照新字段（`remaining_absences`、`channel`、`coverage`、`confidence`、`engine_version`）；旧的 604 条标 `v1-legacy`，趋势图分段。
6. 单测：12 课次 → 缺 4 不挂 / 缺 5 挂；T 为负仍继续扣；停课不计分母。

**验收**：任意一名学生能看到"已缺 N 次 / 还能缺 M 次"，且与手算一致。

### P5 四端鉴权（M）

1. `sys_account(id, login_no, password_hash, role, status)` + 4 个 profile 表；导员账号可用。
2. `TokenService` 改为按 account 签发，token 携带 `role` + 归属 id；4 端登录/刷新/登出一套实现。
3. 权限矩阵逐接口落地：学生=仅自己；教师=仅本人教学班；导员=仅本人行政班（跨教学班聚合）；教务=全校。
4. 通知：`recipient_type` 加 `COUNSELOR`，`recipient_id` 指向 account，加 `aggregate_key`（导员一次一条汇总）。
5. 前端：4 套 layout + 路由守卫。

**验收**：越权访问（用学生 token 查别的学生）返回 403/空；导员能登录并只看到自己的班。

### P6 导员端（L）

1. 首页：名下行政班预警概览（跨教学班、跨课程聚合）。
2. 多科目预警：**≥3 门**（风险分 ≥40）→ 重点关注名单。
3. 班集体提醒：第 1–4 周不触发、只做个体提醒；第 5 周起 `≥30% 且 ≥5 人` → 班会建议；`≥50% 且 ≥10 人` → 重点关注班级 + 抄送教务；**一次一条汇总**。
4. 升级链与留痕：第 1 次 → 无效（下次快照即可判 / 最长 2 周）→ 第 2 次 → 仍无效 → 手动上报教务（有"已 2 次处理记录"提示）→ 教务面谈/帮助 → 期末终判 → 终止干预。
5. `intervention_record` 重构 + 旧 21 行按映射迁移；行政班粒度挂科率反馈。

**验收**：合成/真实数据下能复现"某班第 6 周触发班会提醒、某生 3 门预警进入重点关注"。

### P7 通道 B → 通道 C（L）

1. 通道 B：班内相对排序（出勤扣分绝对量 + 作业提交行为 + 主观分班内序），第 2–3 周起可用。
2. `score_item` 就位后做**分项级 Δ**：`s = μ + α_i + β_(k,major) + γ_t + ε`，交替去均值 5–10 轮、`λ = n/(n+60)` 收缩、clip 0~100、`|Δ|>20` 熔断。
3. 防双重计算：Δ 只用"班 vs 课程平均"；c 只用课程级。
4. 课程四分类 + 输出降级（Ⅳ 类只输出行为预警；低区分度项 `std<3 或 ≥60% 同分` 自动降权）。
5. 置信度 W = 锚点强度 × 数据完备度 × 样本充足度 → 四档输出。
6. 影子分数（折算分不回写 `score_info`）+ 原分/折算对照视图（教务）。
7. **identification check**：用合成数据（已知真值 γ）验证 Δ 恢复误差；对比"按行政班分教学班"与"随机分班"两种情形的偏误。

**验收**：能报出 Δ 的恢复误差；跨班风险排序合理；无锚点的课自动降级而不是硬给分。

### P8 教务端 + 问卷（L）

1. 学期与开课维护：`weeks_per_term × times_per_week` 导入（开学前锁死，范围校验 + 预览确认）、摸底考标记、高考成绩导入（**列为大一 D 层上线的阻塞项**）。
2. 成绩构成：由课程组配置 + **锁定前校验和=100% + 二次确认**；锁定后无修改通道。
3. 考勤归档工作台：接收 → 统一录入/审核 → 生成归档记录；未交名单；覆盖率体检 + 一键催办；**抽查而非逐门审核**。
4. 考前二次确认"实际考核人数"；成绩录入人数核对。
5. 规则版本化（`engine_version + effective_time`）；三级挂科率报表（教学班→老师 / 行政班→导员 / 课程→入历史，按人数加权）。
6. 导员上报的疑难学生 → 面谈/帮助并回写结果。
7. 问卷链路：导员统一收集 → 教务 → 分发授课组全体老师；`questionnaire_flow` 流转状态可见 + 超时提醒。

### P9 合成数据 + 回测（M，与 P2 后任意阶段并行）

3 专业 × 6 行政班 × 40 人 = 720 学生；6 课 × 3 教学班；每生 6 门课；16 周 × 2 次 = 32 课次；考勤明细 ≈ 13.8 万行。注入真值：`α_i`、`γ_teacher`（如 张 −8 / 王 +2）、`c_course`、20% 低录入率老师（模拟 G4/G7）。
产出四张实验表：① Δ 恢复误差 ② 覆盖率→精度曲线（验证 1/2 阈值）③ 分层预警 AUC（仅成绩 / +出勤 / +Δ）④ 通道 A 漏报上界验证。

### P10 AI 功能回收（M，最后）

1. 把工具（`DataQueryAgent` 的 8 个）指向新表，新增考勤明细/覆盖率/Δ/行政班聚合工具。
2. Agent 输出 schema 从"五维风险分"改为"通道 A/B/C + 置信度"；prompt 同步更新。
3. **真值注入扩展**：`remainingAbsences / coverage / confidence / channel` 进 GT，`AgentOutputValidator` 校验模型不得编造确定性疾病数字。
4. 长期记忆扩展到课程/行政班级别；RAG 知识库路由从 `course` 改为 `course_offering`。
5. golden set 用例按新 schema 重写；`agent.enabled=true` 恢复，前端 flag 打开。

---

## 3. 关键路径与并行

- **关键路径**：P0 → P1 → P2 → P3 → P4 → P6 → P7 → P8 → P10
- **可并行**：P5（鉴权）与 P3/P4 并行；P9（合成数据）全程并行；P8 的报表部分可与 P7 并行。
- **最容易被低估的两项**：P2（结构改动会波及几乎所有现有代码）与 P8（教务端的表单/工作台是纯工作量）。

---

## 4. 每个阶段的"最小可演示产物"（创新项目需要随时能演示）

| 阶段 | 演示什么 |
|---|---|
| P0 | 现有系统仍可演示（只是 AI 入口收起），说明"判定逻辑已收回确定性引擎" |
| P3 | 现场 30 秒点一节课的名 → 学生端立刻看到自己的考勤 |
| P4 | 打开某学生 → "已缺 5 次 / 还能缺 −1 → 必然挂科"，并给出"期末需多考 130 分（实际不可达）" |
| P6 | 导员登录 → 一个班 33% 预警 → 收到"建议开班会"；某生 3 门课预警 → 重点关注 |
| P7 | 同一门课三个教学班的风险分布对比（体现 Δ 折算的作用） |
| P8 | 教务端覆盖率体检：某课覆盖率 40% → 一键催办 → 教师端出现待办 |

---

## 5. 与上一份文档的关系

- `下一阶段改造清单.md`：**改什么**（口径 12 条、18 张新表、改造/废弃表、四端功能、接口、测试、风险）。
- 本文：**按什么顺序改、分成哪些阶段、每阶段验收什么、AI 如何安全暂停与回收**。

两份文档的 §1 口径 / §2 结构一经确认即冻结，后续任何变更须升 `engine_version`。
