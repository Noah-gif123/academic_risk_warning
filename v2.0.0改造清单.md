# 学情预警系统 · 下一阶段（v2 重构）改造清单

> 依据：`9月13讨论第四轮.docx`（四轮累积结论）+ 第五次答复（多科目 = 3 门 / 无效窗口采纳默认 / engine_version 粒度采纳）+ 现有库与代码实测（`study_warning_system.sql`，38 张表）。
> 本文只列"要改什么"，不排工期。标记：✅已定 ｜ ⏳待定 ｜ ★本次新锁 ｜ 新增 ｜ 改造 ｜ 废弃

---

## 0. 本轮新锁定的决策

| # | 决策 | 内容 |
|---|---|---|
| ★1 | 导员端「多科目预警」口径 | **3 门及以上**（风险分 ≥40 的课程数 ≥3）才触发"重点关注该学生" |
| ★2 | 「无效」判定窗口 | **下一次快照即可判**，最长观察 **2 周**；期末做一次**终判**（终判无效 → 上报教务 / 终止干预） |
| ★3 | engine_version 粒度 | **规则、权重、公式任一变化即升版本**；不允许只给 `alert_rule_config` 加版本 |

---

## 1. 前置：口径表（所有数字的唯一出处，先写死再写代码）

| 项 | 口径 | 状态 |
|---|---|---|
| 总课次 | `教学周数 × 每周课次`（教务开学前导入锁定）；**分母单位是"课次"不是"周"** | ✅ |
| 允许缺课上限 | `N_max = floor(总课次 / 3)`；12 课次 → 4 | ✅（含新修正） |
| 挂科硬线 | `已缺课 ≥ N_max + 1`（分子只算缺课；迟到/请假不进分子分母） | ✅ |
| 剩余可缺次数 | `N_max − 已缺`；**= 0 → 再缺一次即挂（红）**；**≤ −1 → 必然挂科** | ★修正（原文档 `≤0 即必然挂科`，差一格） |
| 扣分公式 | `P = 2×N缺课 + 1×N迟到 + 1×max(0, N请假 − 3)`；**无上限、不截断**，`T = W − P` 可为负 | ★修正（去掉任何下限/封顶） |
| 及格判定 | `T ≥ 60` **且** `已缺课 ≤ N_max`（两条同时满足） | ✅ |
| 期末需多考 | `(60 − W + P) / r_f`；> 100 → **实际不可达**（通道 A 第三条判据） | ★新增口径 |
| 考勤类型 | 出勤 / 缺课 / 迟到 / 请假（四类必须分开）；整节课未点 → **未点名，不产生记录**（不默认全勤、不默认缺课） | ✅ |
| 点名覆盖率 | `已录入考勤的课次数 / 截至当前应完成的课次数`；应完成课次**只数非停课课次** | ✅（补停课处理） |
| 覆盖率阈值 | `< 1/2` → 提醒任课老师 + 教务；同时该课**置信度判低**；**每两周轻量体检 + 期中正式体检** | ★补充频次 |
| 学生/数据两条线 | 学生线：缺课 > 1/3 挂科；数据线：覆盖率 < 1/2 提醒。**两条线互不干涉** | ✅ |
| 风险档位 | 红 ≥75 ｜ 橙 60–75 ｜ 黄 40–60 ｜ 绿 <40 | ✅ 待回测 |
| 导员班集体提醒 | 第 1–4 周不触发；第 5 周起 `≥30% 且 ≥5 人` → 班会；`≥50% 且 ≥10 人` → 重点关注 + 抄送教务 | ✅ |
| 多科目预警 | 3 门 | ★ |
| 无效判定 | 下次快照 / 最长 2 周 / 期末终判 | ★ |
| engine_version | 规则·权重·公式任一变化即升版 | ★ |

---

## 2. 数据层

### 2.1 新增表（18 张）

| 表 | 关键字段 | 用途 |
|---|---|---|
| `term` | id, year, term_no, start_date, end_date, is_current | 全库时间轴（当前**全库无学期维度**，最大缺口） |
| `sys_account` | id, login_no, password_hash, role(STUDENT/TEACHER/COUNSELOR/ADMIN), status | 四端统一账号 |
| `counselor` | id, account_id, counselor_name, phone | 导员档案 |
| `administrative_class` | id, class_name, major, grade, counselor_id | 行政班 |
| `counselor_class` | counselor_id, admin_class_id | 导员—行政班 |
| `course_group` | id, group_name, term_id, usual_ratio, mid_ratio, final_ratio, locked, locked_by, locked_at | 课程组（成绩构成归属 + 锁定状态） |
| `course_offering` | id, course_id, term_id, group_id, weeks_per_term, times_per_week, total_sessions, has_placement_test, pass_score, course_type | 开课实例（周数/课次分母的真相） |
| `teaching_class` | id, offering_id, teacher_id, class_code | 教学班（1 位老师） |
| `teaching_class_admin_class` | teaching_class_id, admin_class_id | 教学班 ↔ 行政班（多对多） |
| `enrollment` | id, student_id, teaching_class_id, offering_id, term_id, status(ACTIVE/WITHDRAWN/REPEAT/PENDING) | 选课（替代 `student_course`） |
| `course_session` | id, offering_id, week_no, session_no, session_date, status(NORMAL/CANCELLED/ADJUSTED) | **一次课**（覆盖率分母唯一真相） |
| `attendance_record` | id, enrollment_id, session_id, status(PRESENT/ABSENT/LATE/LEAVE), phase(NORMAL/ARCHIVE), source(TEACHER/ADMIN), recorded_by, recorded_at, updated_by/at, archive_id | 考勤明细（双轨） |
| `attendance_archive` | id, offering_id, phase(MID/FINAL), submitted_by, submitted_at, reviewed_by, coverage_rate, status(PENDING/SUBMITTED/REVIEWED/RETURNED) | 归档批次 |
| `score_item` | id, enrollment_id, item_code, item_name, score, full_score, weight, scorer_id, item_type | 分项成绩（分项级 Δ 的数据前提） |
| `student_admission_score` | student_id, province, year, subject_group(物理/历史), total_score, rank, rank_percentile | 入学/高考成绩（D 层） |
| `risk_engine_config` | id, engine_version, channel_gates, signal_weights, level_thresholds, coverage_threshold, effective_time, is_active | 新引擎参数（替代旧 `alert_rule_config` 的角色） |
| `teacher_style` | teacher_id, term_id, item_code, delta, shrink_lambda, sample_size, style_score, style_level, engine_version | 教师风格 Δ |
| `teaching_class_fail_rate` / `admin_class_fail_rate` / `course_fail_rate_history` | 各粒度挂科率 + term_id + engine_version | 三方统计反馈 |
| `questionnaire_flow` | id, offering_id, term_id, stage, holder_id, entered_at, deadline, status | 问卷流转状态（防 4 环节断链） |

### 2.2 改造表

| 表 | 改造内容 |
|---|---|
| `student` / `teacher` / `admin` | 降为 profile，`account_id` 关联 `sys_account`；`student.class_name` → `admin_class_id` |
| `course` | 拆出 group/offering；本体只留课程名、前置课；`usual/mid/final_ratio` 迁到 `course_group` |
| `score_info` | 加 `enrollment_id/term_id`；保留汇总，明细进 `score_item` |
| `homework_info` | 加 `enrollment_id/term_id`；`score_list` 逐步迁 `homework_item` |
| `class_performance` | 降为**汇总缓存**（由 `attendance_record` 重算），不再作为真相 |
| `study_duration` | 加 `term_id`（按周行为信号，喂通道 B） |
| `alert_record` / `alert_snapshot` | 加 `engine_version`、`channel(A/B/C)`、`remaining_absences`、`roll_call_coverage`、`confidence`、`is_void`；旧行标 `v1-legacy` |
| `intervention_record` | 重构：`alert_id` 可空 → `related_alert_ids`(JSON)；`teacher_id` → `operator_id` + `operator_role`；`intervention_type` → `action_type`；新增 `stage`、`effective`；旧 21 行按下表迁移 |
| `notification` | `recipient_type` 加 `COUNSELOR`；`recipient_id` 指向 `sys_account`；新增 `aggregate_key`（导员汇总一条） |
| `admin` / `teacher` / `student` 登录字段 | 密码字段迁 `sys_account.password_hash`（BCrypt），旧明文仅迁移期保留 |

`intervention_record` 旧行迁移映射：`alert_id→related_alert_ids` ｜ `teacher_id→operator_id(+role=TEACHER)` ｜ `intervention_type→action_type` ｜ 旧行统一 `stage=教师处理` ｜ `status: INEFFECTIVE→无效 / COMPLETED→有效 / EXECUTING→待观察` ｜ `result_note→result`。

### 2.3 废弃 / 降级

| 表 | 处置 | 理由 |
|---|---|---|
| `teacher_class` | **废弃** | ID 区间切人（9 行），换班/插班必错；由 teaching_class + 关联表取代 |
| `attendance` | **废弃** | 汇总表、0 行、与 `class_performance` 语义重复 |
| `history_risk` | **停用（保留只读）** | 旧口径派生结论 |
| `alert_rule_config` | **降为只读** | 旧五维引擎参数集（阈值 40/30/15），与新算法不兼容 |
| `student_course` | **迁移后废弃** | 无学期、无教学班归属 |

### 2.4 保留只读（不参与新算法，但不删）

`monitor_report`、`analysis_report`、`strategy_record`、`history_risk`、旧 `alert_*` 历史行、旧 `attendance`。

保留复用（与新算法正交的既有资产）：`agent_run`、`agent_run_step`、`student_memory`、`student_profile`、`student_goal`、`study_plan`、`alert_operation_log`、`teacher_management_log`、`course_knowledge_point`、`knowledge_mastery`、`student_weak_point`、`exercise*`、`sub_question_answer`。

### 2.5 全局约定（新表一律遵守）

1. 所有业务表带 `term_id`（无学期的表不允许存在）。
2. 所有引擎产物带 `engine_version`；趋势/报表按版本分段，**不跨版本连线**。
3. 作废用 `is_void`（退课、重算、误录），不做物理删除。
4. 审计字段：`created_by / created_at / updated_by / updated_at`；改分、改考勤、归档、上报全部留痕。
5. 索引前缀：`enrollment_id`、`offering_id`、`student_id`、`session_id`（考勤明细预计 10 万行级）。

### 2.6 数据迁移

- 旧数据**全量保留**（`alert_record` 164 / `alert_snapshot` 604 / `notification` 1158 / `class_performance` 534 / `study_duration` 83 …），打 legacy 标记。
- 迁移脚本：`student/teacher/admin → sys_account`；`student_course → enrollment`（term 与 teaching_class 需补默认值）；`score_info/homework_info → +enrollment_id`；`intervention_record` 按 2.2 映射。
- 迁移后跑一次**对账**：行数、分数合计、缺课次数合计三项比对。

### 2.7 合成数据生成器（新增工具，非产品功能）

参数：3 专业 × 6 行政班 × 40 人 = 720 学生；6 门课 × 3 教学班；每生选 6 门（≈4320 选课）；16 周 × 2 次/周 = 32 课次；考勤明细 ≈ 13.8 万行。
真值注入：`α_i`（学生能力）、`γ_teacher`（教师尺度，如 张 −8 / 王 +2）、`c_course`（课程因子）、老师点名积极性（含 20% 低录入率老师模拟 G4/G7）、缺课倾向。
用途：Δ 恢复实验、覆盖率-精度曲线、分层预警 AUC、通道 A 漏报率验证。

---

## 3. 鉴权与账号（3 端 → 4 端）

- 新增 `sys_account` + 4 个 profile；`TokenService` 改为**按 account 签发**，token 携带 `role` + 归属 id。
- 登录/刷新/登出 4 端统一一套实现（现为 3 套）。
- **权限矩阵**（逐接口落地）：
  - 学生：仅自己（`student_id` 从 token 取，禁止前端传 id 决定数据范围）
  - 教师：仅自己教学班的学生与课程
  - 导员：仅自己行政班（**跨教学班、跨课程聚合**）
  - 教务：全校；导入、锁定、归档、规则版本、报表
- 通知：`notification.recipient_type` + `aggregate_key`；导员一次一条汇总。

---

## 4. 算法引擎（后端新模块）

| 模块 | 内容 |
|---|---|
| 引擎骨架 | `RiskEngine`（按通道调度）+ `EngineVersion` 管理 + 影子分数（折算分只活在引擎内，**不回写 `score_info`**） |
| **通道 A** | 三条判据：① 已缺 ≥ N_max+1 ② 剩余可缺 ≤ 1 ③ 期末需多考 > 100；输出"确定性风险"独立栏，**不与风险分混算** |
| **通道 B** | 班内相对排序：出勤扣分（绝对量）、作业提交行为（未交率/迟交率）、主观得分班内序（同班同尺度，不需折算）；第 2–3 周起可用 |
| **通道 C** | `s = μ + α_i + β_(k,major) + γ_t + ε` 双向固定效应估 Δ；交替去均值 5–10 轮、约束 Σ=0；收缩 `λ = n/(n+k)`，k=60；**分项级**（依赖 `score_item`）；防双重计算：Δ 只用"班 vs 课程平均"，c 只用"课程级"；边界退化（单教学班 Δ=0、样本<30、新老师、无统考项、clip 0~100、\|Δ\|>20 熔断推教务复核） |
| 锚点融合 | A / A′ / B / C / D / E / F 按置信度加权；Δ 为历史常数（雪球结构） |
| 课程四分类 | Ⅰ/Ⅱ/Ⅲ/Ⅳ 输出降级（Ⅳ 类只输出行为预警 + 低区分度项自动降权：std<3 或 ≥60% 同分） |
| 置信度 W | `W = 锚点强度 × 数据完备度 × 样本充足度`；≥0.7 完整分档 / 0.4–0.7 标注 / 0.15–0.4 仅行为 / <0.15 数据不足；覆盖率 <1/2 直接判低 |
| 风险分公式（★重写） | **两段式**：`z = Σ w_j·g_j(个体信号)` → `R = 100·M(z)`；权重只加在逐学生变化的信号上（启动值：大一 出勤 .40/作业 .30/成绩 .25/主观序 .05；非大一 .30/.25/.30/.15）；Δ、c、成绩构成**只做归一化平移**，不占权重 |
| 快照生成 | 每日快照加新字段；`engine_version` 分段；退课/误录行 `is_void` |
| 预警链路 | 生成 → 升级（多科目 3 门 / 班集体两档）→ 超时 → 关闭；旧 `WarningGenerationService` 仅保留快照机制骨架，五维评分整体废弃 |
| 规则版本化 | `risk_engine_config.engine_version + effective_time`；快照记录版本；跨版本不连线 |
| 回测脚本 | 用合成数据 + 真实历史做参数标定（B1 k、B4 档位、B12/B13 阈值、扣分系数） |

---

## 5. 考勤链路（双轨制 + 归档）

1. **教师端（平时轨道）**：课表驱动"一节课一张点名单"→ 全班默认出勤、只勾异常（缺课/迟到/请假）→ 提交，约 30 秒；Excel 批量导入（学号/姓名/周次/节次/状态，支持多周补录）。
2. **录入即学生可见**：老师录入后学生端**立即**可见自己的考勤明细（不经教务）；同时喂通道 A。
3. **归档轨道**：期中/期末归档窗口开启 → 教师端出现**强制待办** → 提交 → 教务收集统一录入/审核 → 生成**归档考勤**，作为成绩计算与 1/3 门槛的最终依据；未提交持续红点 + 站内信催办，教务端可见未交名单。
4. **覆盖率体检**：每两周轻量 + 期中正式；<1/2 → 提醒老师 + 教务督办列表 + 置信度判低。
5. **状态机（C17）**：平时记录 → 期中归档 → 期末归档；归档后不可改（改需教务且留痕）；退回重交流程。
6. **窗口配置（C18）**：归档周次（建议期中第 9 周 / 期末第 17 周）、时长、是否允许教务延长。
7. **纠错与申诉**：平时记录老师可改但留痕；学生端可查、可申诉（申诉作为数据质量反馈，不直达改数）。
8. **风险提示 G7 的落地口径**：明确"直接发给学生" = **在系统内录入**、学生端即时可见；不能绕开系统另行发送，否则交付预期调整为"期中后才有确定性风险预警"。

---

## 6. 四端功能改造

### 6.1 学生端
课程列表（本学期全部课程）→ 课程详情：自身数据 + 历年挂科情况（脱敏）+ 预警信息；**只看原分，不看折算分**；**考勤明细即时可查**；扣分明细展示（缺课 ×N(−2N)、迟到 ×N(−N)、请假 ×N(−max(0,N−3))）；明确区分两条线（1/3 是硬门槛，迟到/请假只扣分）。
⏳ 待定：是否展示"确定性风险/剩余可缺次数"（怕诱导算着缺课）；历年挂科展示形式（百分比 / 档次）；推送方式（站内信 / 邮件 / 无）。

### 6.2 教师端
课程列表 → 教学班学生按危险度排序；**成绩构成配置**（课程组开学统一确定 + 锁定前强制校验和=100% + 二次确认弹窗 + 锁定后不可改）；考勤录入（见 §5）；期中/期末**强制归档待办**；**点名覆盖率提示**「已点名 N / 应点名 M」；教学班挂科率反馈；共识难点对比（我的班 vs 同课程其他教学班）；问卷直收（学生知识点/难点反馈直达任课教师）。
⏳ 待定：老师能否看自己的风格分 S/A/B/C；课程组内构成未达成一致的裁决人；默认模板是否要做；老师能否看到被熔断标记的异常 Δ；归档周次确认；归档提交后老师能否改。

### 6.3 教务端
保留：预警规则配置。删除：成绩构成功能（下放课程组）。新增：入学/高考成绩导入与维护；教学周数/每周课次导入（开学前定死）；课程初始化标记摸底考；**考勤归档工作台**（接收 → 统一录入/审核 → 生成归档记录 + 未交名单）；**点名覆盖率体检 + 一键催办**；考前二次确认"实际考核人数"；成绩录入人数核对；规则版本化（rule_version + 生效时间）;导员上报疑难学生 → 面谈/帮助并**记录结果**；三级挂科率报表（教学班/行政班/课程）；原分 + 折算分对照视图。
⏳ 待定：对照视图是否落库（倾向"给视图不落库"）；考核人数确认的时间点与界面；风格分维护是手动还是"系统建议+人工确认"；归档窗口开启/关闭由谁定、可否调整；期中归档是照单全收还是逐门审核（建议：只做覆盖率体检 + 抽查）。

### 6.4 导员端（**新建第四端**）
首页形态待设计；核心三块：① **多科目预警**（≥3 门）→ 重点关注；② **班集体提醒**（第 5 周起，≥30% 且 ≥5 人 → 班会建议；≥50% 且 ≥10 人 → 重点关注班级 + 抄送教务；第 1–4 周不触发，只做个体提醒）；③ **一次一条汇总**（覆盖名下全部行政班，不一课一条、不一班一条）。
升级链：第 1 次提醒/谈话 → 无效 → 第 2 次处理 → 仍无效 → 导员判定"收集齐全"**手动上报教务** → 教务面谈/具体帮助 → 再无改善 → 终止干预；全程写 `intervention_record`；上报后可追踪教务处理状态。
其它：行政班粒度挂科率反馈；"可上报"辅助提示（该生已有 2 次处理记录）。
⏳ 待定：导员是否能看到学生单科预警详情（还是只给多科聚合）；通知是否需要学生/老师回执。

### 6.5 问卷 / 反馈子系统
流转（已拍板）：导员统一收集该科目问卷 → 教务 → 教务分发给该课程**授课组全体老师**；补丁：`questionnaire_flow` 流转状态可见 + 超时提醒。
产出：① 老师评价 ② 知识点讲解反馈。

---

## 7. 智能体层对齐（保留成果、重新指向）

1. **分工边界写死**：确定性判定（挂科线、扣分、覆盖率、置信度、Δ/c）**只由规则引擎算**；大模型只做解释、建议、归因、问答。
2. **真值注入扩展**：把 `remainingAbsences / rollCallCoverage / confidence / channel / riskScore / predictedScore` 作为 ground truth 注入上下文，`AgentOutputValidator` 校验模型不得编造（复用现有 GT 机制）。
3. **工具改造**：`DataQueryAgent` 的 8 个工具改指向新表，并新增：考勤明细、覆盖率、Δ 与折算对照、多科目预警、行政班聚合。
4. **各 Agent prompt/schema 更新**：Monitor 的"五维风险分"改为"通道 A/B/C + 置信度"；Strategy/Feedback 的输入随策略链（含干预记录）变化；Profile 三维画像输入改为新考勤/分项/Δ。
5. **长期记忆扩展**：从"学生级"扩到"课程/行政班级别"（导员侧记忆）。
6. **RAG 路由**：课程知识库 appId 路由从 `course` 改为 `course_offering`（同一课程可能有多个开课/教师）。
7. **评测扩充**：golden set 用例改成新输出 schema；消融统计继续保留（反思组 vs 直达组）。

---

## 8. 接口清单（新增/改造）

| 端 | 新增/改造接口 | 说明 |
|---|---|---|
| 通用 | `/api/auth/login|refresh|logout`（4 端统一）、`/api/account/me` | 替换现三套登录 |
| 学生 | `/api/student/courses`、`/courses/{offeringId}`、`/attendance`（明细）、`/deduction`（扣分明细）、`/alerts`、`/history-fail`、`/appeal` | 全部按 token 限定本人 |
| 教师 | `/teacher/offerings`、`/teaching-class/{id}/students`、`/score-config`（课程组配置+锁定）、`/attendance/session/{sessionId}`（点名单）、`/attendance/import`、`/attendance/archive/{phase}`（提交）、`/coverage`、`/fail-rate`、`/consensus-difficulty` | 限本人教学班 |
| 教务 | `/admin/terms`、`/admin/course-groups`、`/admin/offerings`（周数课次/摸底考/构成锁定）、`/admin/admission-scores/import`、`/admin/attendance/archive-workbench`、`/admin/coverage`（体检+催办）、`/admin/exam-headcount`、`/admin/rule-versions`、`/admin/reports/{level}`、`/admin/score-view`（原分+折算对照）、`/admin/referrals`（导员上报处理） | 全校 |
| 导员 | `/counselor/dashboard`、`/counselor/students`（多科目预警 ≥3）、`/counselor/classes`（预警占比）、`/counselor/interventions`（录入/查询）、`/counselor/refer`（手动上报）、`/counselor/fail-rate` | 限本人行政班 |
| 引擎 | `/engine/snapshot/rebuild`、`/engine/delta/recompute`、`/engine/config`（版本化） | 运维/教务 |
| 智能体 | `/api/agent/*` 保持，`/api/agent/ask` 工具改指新表 | 复用现有 |

---

## 9. 前端工程

- 路由与守卫：3 套 → 4 套（student / teacher / admin / counselor），新增导员端 layout 与页面。
- `api/http.js`：按端拆分 token 与 401 刷新；新增上表全部接口封装。
- 新增组件：课表驱动点名单、考勤 Excel 导入、归档待办与工作台、覆盖率体检面板、扣分明细卡、导员聚合看板与升级链时间线、原分/折算对照视图、问卷流转状态。
- 复用组件（不动或小改）：`ThreeDimProfile`、`RiskRadarChart`、`RiskTrendChart`、`AgentRunHistory`、`AgentToolAsk`、`StudentChatAssistant`、`ChatAssistant`。
- 趋势图改造：按 `engine_version` 分段、断点标记，避免新旧口径混画。

---

## 10. 定时任务、配置、审计、性能

- 定时任务：每日全量快照（含无预警学生的 GREEN）；超时升级与提醒；**每两周覆盖率体检**；归档窗口开启/关闭；催办（红点 + 站内信）；Δ 定期重估（学期间）。
- 配置项（`application.properties` 新增，均带默认值）：引擎版本、通道开关、档位阈值、覆盖率阈值(0.5)、导员阈值(0.30/5、0.50/10)、多科目门限(3)、无效窗口(2 周)、扣分系数(2/1/1/3)、λ 的 k(60)、熔断阈值(20)、归档周次(9/17)。
- 审计：改分、改考勤、归档、退回、上报、终止干预全部留痕（表 + 操作日志）。
- 性能：考勤明细 10 万行级 + 快照 720×6/天 → 需覆盖索引与批量插入；快照全量生成用分批（现为逐生逐课写）。

---

## 11. 测试与回归

- 现有 133 个单测：纯逻辑用例（阈值、边界、公式）保留作回归网；涉及旧五维评分/旧画像口径的用例随口径作废重写。
- 新增必测边界（每一条都要用例）：
  1. 1/3 线：12 课次 → 缺 4 不挂、缺 5 挂；剩余=0 显示"再缺一次即挂"。
  2. 扣分无上限：T 为负仍继续扣；扣分致挂科与硬线挂科两条路径都成立。
  3. 覆盖率分母：停课课次不计入分母；未点名不产生记录。
  4. 双轨冲突：归档优先于平时（判定用归档，预警用平时），冲突时以归档为准。
  5. Δ：单教学班/样本<30/新老师/无统考项/熔断 五种退化路径。
  6. W 分档四档边界。
  7. 导员聚合：第 1–4 周不触发；30%/5 人与 50%/10 人两档；多科目 3 门。
  8. 引擎版本：跨版本不连线、旧行 legacy 标记。
- 迁移对账测试：行数与汇总值比对。

---

## 12. 文档与论文产出

- 《口径表（Decision Table）》——本文 §1 定稿版。
- 新 ER 图 + 数据字典（38 → 约 55 张表）。
- 算法说明：通道 A/B/C、Δ 估计与收缩、锚点融合、置信度、两段式风险分。
- 实验表：① Δ 恢复误差随样本量/k 变化 ② 覆盖率→精度曲线（验证 1/2）③ 分层预警 AUC（仅成绩 / +出勤 / +Δ）④ 通道 A 漏报上界的形式化证明（前提：录入无系统性误标）。
- 演示脚本更新：四端流程 + 新旧对照。

---

## 13. 风险登记（保留 + 新增）

| # | 风险 | 缓解 |
|---|---|---|
| G1 | 成绩构成锁死后无法挽回 | 锁定前校验和=100% + 二次确认；锁后修正只走数据库运维 |
| G2 | 周数/课次一次性录入错 → 影响 1/3 分母 | 导入范围校验 + 预览确认 + 教师端可见 |
| G3 | 高考分未按期导入 → 大一 D 层为空 | 列为上线阻塞项 + 导入进度提示 |
| G4 | 老师不按时录入 → 通道 A 形同虚设 | 30 秒点名单 + 待办红点 + 覆盖率催办 |
| G5 | 学生质疑"迟到多为何不预警" | 学生端明示两条线 |
| G6 | 问卷 4 环节断链 | `questionnaire_flow` 状态可见 + 超时提醒 |
| G7 | 老师绕开系统发考勤 → 通道 A 退化 | 口径写死"直接发给学生 = 系统内录入"；否则调整交付预期 |
| G8 | 归档"照单全收"权威性不足 / 逐门审核不可承受 | 只做覆盖率体检 + 抽查；<1/2 进待督办 |
| ★G9 | 无学期维度 → Δ/历史无从积累 | 新库全表带 `term_id`，先建 `term` |
| ★G10 | 教学班按行政班整班划分 → 教师效应与班级能力相关 | 合成数据做 identification check；用 α_i 剥离个体差异 + 入学分作协变量 |
| ★G11 | 扣分无上限可能比 1/3 更严 | 用合成数据统计"扣分致挂科"占比，再定 2/1 系数是否调整 |

---

## 14. 开工顺序（只看依赖，不排工期）

1. **口径表定稿**（§1）——所有后续工作的唯一基准。
2. **`term` + 班级/开课/选课结构**（`term`、`course_group`、`course_offering`、`teaching_class`、`administrative_class`、关联表、`enrollment`）——新算法的地基。
3. **`course_session` + `attendance_record` + `attendance_archive` + 双轨录入**——通道 A 的唯一输入，优先级高于算法打磨。
4. **通道 A**（判据三条 + 扣分公式 + 硬门槛，可用现有 `class_performance.absent_count` 先跑原型）。
5. **四端鉴权**（`sys_account` + 权限矩阵）。
6. **导员端**（行政班聚合 + 升级链 + `intervention_record` 重构与迁移）。
7. **通道 B** → **期中后通道 C**（Δ 估计 + 收缩 + 分项级，依赖 `score_item`）。
8. **教务端**（导入/锁定/归档工作台/报表/对照视图）与**问卷链路**。
9. **智能体层对齐** + 工具改指新表。
10. **合成数据 + 回测 + 论文实验表**（可与 3–7 并行）。

---

*本文为下一阶段改造的总清单；口径与结构部分（§1、§2）一经确认即视为冻结，后续变更须升 `engine_version`。*
